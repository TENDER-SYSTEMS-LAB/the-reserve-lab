# Wiki Log

Append new entries after all existing entries. Do not rewrite past entries.

## [2026-09-06] maintenance | Initialize THE RESERVE Wiki

- Created the repository entry point, agent instructions, schema, raw-source workflow and registry, Wiki index, and current-state snapshot.
- Registered `SRC-2026-09-06-wiki-initialization-request` as a verbatim capture of the user's task message and promoted its explicit initialization request into current state.
- Adapted operational documentation from the local OTHER GOODS and LONGING checkouts. Reference HEADs at inspection: OTHER GOODS `817e88f0a150c2ebc1508d1233297abc052bf3a2`; LONGING `7408711818174acabc05795ae88d9c472745de14`. These identify the inspected checkouts; no project source corpus was imported.
- Preserved English maintained documentation, original-language raw evidence, source hashes, explicit attribution and status, and append-only history. Kept raw capture corrections as separate sources consistently across the rules.
- Selected `the-reserve-lab` to match the local directory and public visibility to match the two reference repositories. These are initialization choices, not creative project decisions.
- The project's concept and scope remain undocumented. No placeholder concept, decision, or question pages were created.

## [2026-09-06] maintenance | Align README with OTHER GOODS

- Applied the user-provided OTHER GOODS README structure: project title, short introduction, divider, institutional credit, and Archive link.
- Removed repository-operation details from the entry point. Kept the introduction limited to the documented project affiliation and development state; no concept tagline or project identifier was invented.

## [2026-09-06] ingest | Register bank interpretation and naming conversations

- Renamed the two user-supplied exports at the user's request, preserving their complete contents byte-for-byte:
  - `ChatGPT-은행 작품 해석-20260906-1957.md` → `2026-09-06-bank-artwork-interpretation.md`; Git blob hash `b907053cadbbf9b5f27e62adb76d23265577d8cc`.
  - `ChatGPT-작품 이름 논의-20260906-1957.md` → `2026-09-06-project-naming-and-public-language.md`; Git blob hash `4850e08b17999d7214641ff57069b29e697ff019`.
- Registered both sources and their hashes. Promoted five institutional alternatives and the recommended future-claims mechanism as `llm-proposed`; recorded the missing source attachment and unverified financial explanations.
- Recorded THE RESERVE and “A place for what remains.” as explicit user decisions. Kept project codes, handles, mechanisms, and worldbuilding suggestions unconfirmed.
- Added an overview, two concept pages, two decision pages, and one consolidated question page; updated current state and index while preserving the earlier initialization history.
- Rewrote the README using the confirmed title and bio, with Overview and Archive navigation. Removed TENDER SYSTEMS from README at the user's request. The removal applies to README, not to immutable sources or institutional Git rules.

## [2026-09-06] ingest | Preserve the design audit and proposed worldbuilding roadmap

- Registered the user's audit brief and the delivered Korean design audit as byte-identical standalone originals, plus the follow-up ingestion request as a verbatim message capture with a terminal newline. The source registry records their hashes, distinct attribution, and promotion roles.
- Preserved the audit's readiness findings and its 2026-09-06 22:39 KST related-repository snapshot, including uncommitted-source, access, and secondary-evidence limits. The audit request does not replace the earlier missing bank-interpretation attachment.
- Added one independently developing concept, worldbuilding-roadmap, for the proposed scope/depth framework, six completion gates, reopening triggers, A–F dependencies, and five assignable review tasks. Reused institutional-directions for candidate and cross-work comparisons, future-claims for the unresolved benefit and five test cases, and Q-001 for prioritized user choices.
- Updated overview, current state, and the index together. Ingestion authorization is recorded separately from adopting an institution, scope, currency, rule, or implementation plan. Existing name and bio decisions remain unchanged; no candidate was selected or rejected by this ingestion.
- Verified all three previously registered source hashes before writes and all six registered hashes after synthesis. Both copied files remain byte-identical to their supplied originals. Checked Wiki/source links, one index entry per Wiki page, attribution, and Git whitespace; no findings. The earlier LONGING CSV normalization signal remains a caveat in the report, not a new THE RESERVE source mismatch.
- Appended this entry without altering prior log content. No artwork implementation, issue creation, commit, or push was performed.

## [2026-09-06] lint | Align source and index links with LONGING

- Wiki `## Sources` entries used per-page relative Markdown links to the raw files. Rewrote them as `[[SRC-...]] — raw/<type>/<file>.md`, the form `schema.md` documents and LONGING uses, so pages resting on the same original share a link.
- `wiki/index.md`: added the standard page frontmatter it was missing and converted its internal page links to `[[wikilink]]` form. Repository-control links to `../README.md`, `../AGENTS.md`, `../schema.md`, and the raw files remain Markdown links, as in LONGING.
- No claim, attribution, status, or hash changed.

## [2026-09-06] lint | Link source files from every Sources entry

- Following the alignment entry above, the 25 `## Sources` entries across 8 pages now read `[[SRC-...]] — [raw/<type>/<file>.md](<relative path>)`. The wikilink is the shared source identifier; the Markdown link restores the direct route to the file that the earlier per-page links provided.
- `schema.md`: the Provenance example now shows both links and states the relative-path rule — `../raw/...` from a page directly under `wiki/`, `../../raw/...` from a page in a subdirectory.
- No claim, attribution, status, or hash changed. This is a link-format correction only.

## [2026-09-07] lint | Verify provenance links and synchronize maintenance dates

- Reviewed the pending documentation changes and synchronized the eight edited subject pages, index, and corresponding catalog dates for this maintenance. Project claims, statuses, attribution, and source registrations remain unchanged.
- Corrected the count in the preceding entry by addition: there are 23 `## Sources` entries across 8 pages, not 25. The earlier entry remains intact as part of the append-only history.
- Verified one catalog entry for each of the nine Wiki pages other than the index, matching page statuses and dates, registered source identifiers, and existing local link targets. `git diff --check` passed, and `git status --porcelain raw/` was empty; registered originals were not changed or rehashed.

## [2026-09-07] maintenance | Reduce routine context and verification work

Replaced mandatory full-file startup with targeted catalog/page reading and on-demand schema, source, and history lookup. Shortened agent instructions while retaining institutional commit identity and each repository's standing push authorization. Removed the schema's conflicting periodic hash-audit rule: register a new source's hash once, inspect changed raw paths for routine edits, and reserve a complete provenance audit for an explicit request.

Scoped lint and catalog updates to affected material and limited logging to durable results. Kept raw guides procedural and source-registry rows unchanged; registered originals and previous log entries are preserved. Updated only the affected repository-control catalog entries. Git-only work can reuse an unchanged completed review.

## [2026-09-07] decision | Confirm RS-001 — THE RESERVE

Registered the selected approval exchange as SRC-2026-09-07-the-reserve-identifier-confirmation. The user confirmed RS-001 — THE RESERVE and requested README and Wiki updates followed by commits and pushes in both repositories. Updated the relevant public entry point, identity records, current state, and catalog; preserved the earlier proposal history and the unconfirmed social handle and operating model. TENDER SYSTEMS records the active system in its registry; the Lab retains the canonical naming decision.

## [2026-09-10] ingest | Register currency direction and R protocol / whitepaper conversations

- Renamed three unregistered user-supplied exports without changing their bytes; registered one Git blob hash per new source:
  - `ChatGPT-통화 가치 표현-20260910-1227.md` → `2026-09-09-currency-value-supply-and-velocity.md`.
  - `ChatGPT-R 암호화폐 발행!-20260910-1228.md` → `2026-09-10-r-token-issuance-and-protocol-drafts.md`.
  - `ChatGPT-암호화폐 백서 작성 비교-20260910-1228.md` → `2026-09-10-r-whitepaper-structure-comparison.md`.
- The currency source began September 9 and continued September 10; its filename uses the start date while original timestamps remain untouched. Recorded capture and attribution limits, unsupported summary additions, and conflicting draft version labels in the registry.
- Recorded the user-confirmed bank/currency direction, fixed-unit and shrinking-supply premise, corrected storage preference, declining velocity, and audience AI-language boundary. Preserved R/RES/RSV ambiguity without adopting a ticker.
- Added monetary-direction, R-protocol, and whitepaper-development pages; updated the existing institutional, credit, roadmap, scope, overview, current-state, and catalog entries. Kept treasury rules, parameters, network, observer, rate model, and later TOC as proposals; ingestion and drafting requests do not authorize issuance or establish implementation.
- Preserved the prior audit and task cards as dated proposals, the explicit behavioral correction, and unresolved tensions between fixed internal value/external price, planned contraction/available reserves, authored decline/observed velocity, and the two rate narratives. No sibling repository, public README copy, contract, deployment, commit, or push was changed by this task.
- Verification passed: rename-time byte comparisons, new-source registration, changed-page local/source links and catalog metadata, new-page whitespace, Git diff whitespace, and mechanical preservation of all prior log bytes. The scoped raw status showed only the registry and three new exports; no previously registered original was modified or rehashed.

## [2026-09-10] draft | Write White Paper v0.2 Sections 1–4 and record the exposed definitions

- Added a `whitepaper/` layer for work products and drafted [R White Paper v0.2](../whitepaper/R-white-paper-v0.2.md) — Abstract, Introduction, Design Principles and Non-Goals, and Definitions and System Model as whitepaper prose, labeled Document 0.2 / Protocol 0.1 / Draft. Sections 5–22 remain outline, each annotated with the gap blocking it.
- The drafting exposed twenty undefined parts of R, recorded in the manuscript as `OPEN-01`–`OPEN-20` and grouped by consequence: five block later sections, seven are structural inconsistencies in the model, and eight bound what the document may claim.
- Two gaps were not visible in the earlier outline. Settlement is permissionless while release is institution-authorized, so R is neither decentralized nor conventionally administered and Section 14 cannot be written until that hybrid is named. The treasury is also unpartitioned while serving both release and retirement, so release policy silently determines whether the contraction schedule is achievable.
- Resolved the recorded `S` notation collision inside the manuscript only: `S` and `T` stay monetary, protocol state is `Σ(t)`, transition is `τ`. This is a drafting decision, not a user-approved convention.
- Updated r-whitepaper-development with the drafting result and findings, Q-001 with the additional missing definitions, current state's next-work section, and the affected catalog entries; registered the new manuscript under a Manuscripts heading and extended the editing-destination note.
- No confirmed parameter, approved release, implementation, deployment, or issuance follows from this drafting. No registered original was modified; `git status --porcelain raw/` was empty and `git diff --check` passed.

## [2026-09-10] decision | Confirm central-bank role and authored declining velocity; draft Sections 5–9

- Registered the decision turn as `SRC-2026-09-10-whitepaper-sections-1-4-decisions` with one Git blob hash; it is a selected-turn capture, not a full export.
- Recorded [DEC-004](decisions/DEC-004-monetary-authority-and-authored-decline.md): within the world THE RESERVE is the central bank and issuing authority of R (`OPEN-02`), and the declining velocity is authored world history that the protocol cannot control (`OPEN-03`). The user accepts explicitly that observed on-chain velocity may diverge.
- Applying the decisions resolved two further manuscript gaps without a separate decision. `OPEN-04` draws the artwork/protocol boundary by enforceability rather than by section number, with world records labeled wherever they appear. `OPEN-05` is half-resolved: decentralization is not a goal of R, since an authorized release alongside permissionless settlement is the intended structure; the permission model, keys, and release bounds remain open.
- Drafted whitepaper Sections 5–9 — The Monetary Unit, Monetary State, Issuance and Supply, Transactions and State Transitions, and Monetary Dynamics. Section 9 carries the manuscript's only world records and states the divergence rule: where measurement contradicts the world record, neither is adjusted to fit the other. Sections 10–22 remain outline, each annotated with the gap blocking it.
- Four further undefined parts were exposed and recorded as `OPEN-21`–`OPEN-24`: whether R is a liability of THE RESERVE and whether the institution has a balance sheet; whether the same institution operates the banking layer or licenses separate banks; the mandate of a central bank whose only discretionary act is release; and whether the world record carries stated values or only a direction. The register now holds twenty-four items, four resolved.
- Updated r-whitepaper-development, r-monetary-protocol's velocity tension, Q-001, DEC-003's cross-reference, current state, and the affected catalog entries.
- No parameter, release, implementation, deployment, or legal classification follows from these decisions. Verification passed: new-source registration, changed-page local and source links, catalog metadata, manuscript heading structure, and `git diff --check`; no previously registered original was modified or rehashed.

## [2026-09-10] decision | Confirm assumed base, single-institution banking layer, and the RESERVE mandate; draft Section 10

- Registered the decision turn as `SRC-2026-09-10-monetary-unit-banking-layer-and-mandate` with one Git blob hash; it is a selected-turn capture, not a full export.
- Recorded [DEC-005](decisions/DEC-005-monetary-unit-banking-layer-and-mandate.md): R is the world's monetary unit with a quantity assumed to have existed from the outset (`OPEN-21`), the same institution operates the banking layer under a stated principle of not expanding the world unnecessarily (`OPEN-22`), and the mandate is RESERVE (`OPEN-23`).
- The assumed base resolves `OPEN-21` and reframes genesis throughout the manuscript: R is not a liability, is issued against nothing, and the institution has no balance sheet in which R is an obligation. Genesis records the assumed quantity rather than creating it, so the institution's authority begins with a stock it did not make. "Issuing authority" now means only that no one else may bring R into existence.
- The mandate also resolves `OPEN-01`, which had been open since the first drafting pass: the institution's purpose and its monetary objective are the same word.
- Added design principle `P7`, no unnecessary expansion, and drafted whitepaper Section 10, Monetary Policy — an objective, one instrument, and what policy cannot do. Sections 11–22 remain outline.
- Three further gaps were exposed and recorded as `OPEN-25`–`OPEN-27`: whether the banking layer may create claims exceeding the base it holds; whether release can ever be obligatory rather than discretionary; and confirmation of the reading under which a mandate to keep is consistent with a mechanism that destroys money, namely that retirement removes only the undistributed remainder so that what is preserved is exactly what people hold. That reading is recorded in the manuscript as an inference, not as the institution's own account.
- The register now holds twenty-seven items, nine resolved. `OPEN-13`, release eligibility, is the last of the original blocking gaps.
- Updated r-whitepaper-development, r-monetary-protocol's layer separation, Q-001, DEC-004's cross-reference, current state, and the affected catalog entries.
- No parameter, release, implementation, deployment, banking licence, or legal classification follows from these decisions. Verification passed: new-source registration, changed-page local and source links, catalog metadata, manuscript heading structure, and `git diff --check`; no previously registered original was modified or rehashed.

## [2026-09-10] decision | Correct the mandate reading, record the burn mechanism, and refine genesis

- Registered two new sources: `SRC-2026-09-10-burn-mechanism-and-value-preservation` (`b5a2b64d`) and `SRC-2026-09-10-initial-issuance-clarification` (`0e1b2068`). Both are selected-turn captures, not full exports.
- Recorded [DEC-006](decisions/DEC-006-burn-mechanism-and-value-preservation.md). The user **corrected** the assistant's `OPEN-27` reading rather than confirming it. The earlier reading — that retirement removes only the institution's undistributed remainder, so what is preserved is what people hold — was wrong; it described a boundary where the answer is a purpose. R's value is falling in the world and destruction is how that value is preserved, so the mandate to keep and the mechanism that destroys money share one purpose.
- The same turn supplied the missing mechanism: use between people leaves total supply unchanged, while R used toward the system is burned. The model now has two supply-reducing channels. The manuscript's earlier invariant `I2`, that settlement alone reduces supply, was false and has been corrected; Section 7.7 was added, the operations table gained `systemPayment`, the account classification gained a system kind, and the velocity measure now excludes System Payments because they are the destruction channel rather than circulation.
- `OPEN-14` and `OPEN-27` are resolved by that turn. Four gaps follow: whether burn counts toward the contraction target (`OPEN-28`), whether the whole amount is burned and what the payer receives (`OPEN-29`), who designates a system account now that classification decides whether R is destroyed (`OPEN-30`), and how the falling value is held together with the confirmed fixed-unit premise in DEC-003 (`OPEN-31`, recorded as an inference awaiting confirmation).
- Corrected the superseded reading in place in [DEC-005](decisions/DEC-005-monetary-unit-banking-layer-and-mandate.md), retaining it as the question's history rather than as a current claim.
- A clarifying note then refined genesis: R was first issued at a specific past point in a fixed quantity, with no issuance since. `t = 0` therefore precedes the reader and the decline is already under way when the work is met. Recorded as a refinement within DEC-005. Two further gaps followed: no date, elapsed interval, or current epoch is supplied (`OPEN-32`), and a deployed contract's own genesis is necessarily the moment of deployment rather than `t = 0` (`OPEN-33`).
- The register now holds thirty-three items, eleven resolved. Updated r-whitepaper-development, r-monetary-protocol, Q-001, DEC-005, current state, overview, and the affected catalog entries.
- No parameter, date, quantity, release, implementation, deployment, or economic validation follows from these turns. That destruction preserves value is a premise of the world, not a demonstrated result. Verification passed: new-source registration, changed-page local and source links, catalog metadata, manuscript heading structure, and `git diff --check`; no previously registered original was modified or rehashed.

## [2026-09-10] decision | Confirm burn accounting, system-account authority, in-world-only holding, the fixed-unit reading, and the 1975 genesis date

- Registered the decision turn as `SRC-2026-09-10-burn-accounting-payer-and-genesis-date` (`b6a6900f`); a selected-turn capture, not a full export. Recorded [DEC-007](decisions/DEC-007-burn-accounting-payer-and-genesis-date.md).
- Burn counts toward the contraction target. The settlement formula needs no special case, so once burn carries supply below target the required retirement is zero. `OPEN-18` resolves as a consequence: the target is a **ceiling**, not a floor.
- THE RESERVE designates system accounts. Because a transfer to one is destroyed, this is a monetary power rather than bookkeeping, and it settles the classifying authority in `OPEN-09`. The institution now has two discretionary acts, release and classification; the manuscript's earlier claim that release is its only monetary act was corrected in §8.1 and §10.2.
- `1R = 1R` is confirmed as the result of the burn mechanism rather than an independent axiom, resolving `OPEN-31`. The fixed-value premise in DEC-003 is explained rather than revised.
- Genesis is 17 September 1975, Epoch 0, with the present at approximately 50% of the initial quantity.
- The statement that R exists only within the world and cannot be held by an actual person reaches beyond the question it answered. It resolves `OPEN-06` — no external market is possible — and makes every holder and account in the manuscript an in-world entity, with the audience observing rather than participating. Sections 1, 3.4, and 5.4 were rewritten accordingly.
- Two conflicts are recorded rather than resolved. `OPEN-34`: over the elapsed 50.98 years — 18,621 days, 2,660 seven-day epochs — a 3% annual contraction leaves 21.16% of the initial quantity, not 50%; approximately 1.35% annually, or 0.026% per epoch, produces 50%, and burn counting toward the target complicates even that. `OPEN-35`: DEC-004 accepts that real participants would produce observable on-chain velocity, which cannot happen if no real person can hold R; the conflict is cross-referenced in DEC-004 and leaves the Reserve Observer without a stated subject.
- `OPEN-33` was reframed rather than answered: a deployment's own genesis cannot be 1975 and it would have no one to distribute to, so what a public deployment is for now blocks Section 19.
- The register holds thirty-five items, nineteen resolved. Updated r-whitepaper-development, r-monetary-protocol's parameter note, Q-001, DEC-004's conflict cross-reference, current state, overview, and the affected catalog entries.
- The arithmetic above is computed from the confirmed dates, not supplied by the source. No contraction rate, quantity, on-chain parameter, release, implementation, or deployment follows from these decisions. Verification passed: new-source registration, changed-page local and source links, catalog metadata, manuscript heading structure, and `git diff --check`; no previously registered original was modified or rehashed.

## [2026-09-10] decision | Confirm an accelerating noisy contraction, simulated flows, and the ledger's scope and starting point

- Registered the decision turn as `SRC-2026-09-10-contraction-curve-simulation-and-ledger-scope` (`5d102e04`); a selected-turn capture, not a full export. Recorded [DEC-008](decisions/DEC-008-contraction-curve-simulation-and-ledger-scope.md).
- The contraction is **not** a constant exponential. It is slow at first and accelerates, and its realized path is noisy. The draft 3% annual rate is therefore the wrong shape as well as the wrong magnitude. Section 7.4 was rewritten around a stretched exponential fitted to the confirmed 50% anchor, with draft `k = 2`, `λ = 61.24` shown as a table from 1980 to 2075 and marked unconfirmed; only the anchor is confirmed.
- Added §7.4a for the noise, including the constraint it cannot break: total supply never rises, so noise appears only as variation in the speed of decline and never as an upward fluctuation. A noise model that can increase supply is inadmissible.
- There are no real participants and the flows are **simulated**. This resolves `OPEN-35` and supersedes the second half of DEC-004, which had accepted real on-chain participation and a possible divergence. §9.3 was rewritten from "Observation and divergence" to "Simulation"; the divergence rule is removed because nothing independent exists to diverge from. The Reserve Observer publishes a generated series, labeled as such, and the honesty requirement moves from separating measurement and claim to disclosing that the series is generated. DEC-004 now records the supersession.
- The deployment is **both** a public ledger of the world's monetary history and a rule implementation whose accounts exist only within the world, distributing to no one. `OPEN-33` resolved.
- The elapsed decline since 1975 sits **before the record**. The ledger opens at roughly half of `S₀` and at approximately epoch 2,660, not at `S₀` and epoch 0; the original quantity remains a documented fact of the world's history rather than an opening balance.
- Three gaps followed. `OPEN-37`: the anchor fixes `λ` once `k` is chosen but does not choose `k`, and the curve is now the world's whole monetary history. `OPEN-36`: the noise has no distribution, amplitude, or time scale, and a process centred on a curve that is also a ceiling would breach it, which may reopen `OPEN-18`. `OPEN-38`: what generates the simulated data and whether the generator is disclosed.
- The register holds thirty-eight items, twenty-two resolved. Updated r-whitepaper-development, r-monetary-protocol's parameter and velocity notes, Q-001, DEC-004's supersession, current state, overview, and the affected catalog entries.
- The curve table is computed from the confirmed anchor and dates, not supplied by the source. No functional form, shape parameter, quantity, deployment target, or implementation follows from these decisions. Verification passed: new-source registration, changed-page local and source links, catalog metadata, manuscript heading structure, LaTeX integrity in the rewritten schedule, and `git diff --check`; no previously registered original was modified or rehashed.

## [2026-09-10] decision | Add a 2025 path point, bound the noise, settle the generator, and withdraw non-expansion

- Registered the decision turn as `SRC-2026-09-10-second-anchor-noise-bounds-and-generator` (`32205549`); a selected-turn capture, not a full export. Recorded [DEC-009](decisions/DEC-009-second-anchor-noise-bounds-and-generator.md).
- 30% of the original quantity remains at 17 September 2025, exactly fifty years after genesis. This is earlier than the previously confirmed 50% at 10 September 2026, so the two cannot lie on one falling curve. Since the same turn permits intermediate rises, the manuscript reads the 2026 figure as pinning the **schedule** and the 2025 figure as a point on the **realized path**, about 21 percentage points beneath the ceiling before returning to touch it within a year. Recorded as an interpretation needing confirmation, not as a settled fact.
- A consequence corrects an earlier expectation in this repository: a second point on the realized path does **not** fix the schedule's shape parameter. `OPEN-37` now needs a second point on the ceiling, not one more dated figure.
- Noise is one-sided beneath the schedule and bounded above by the quantity issued at genesis. This settles the structural half of `OPEN-36` without reopening `OPEN-18`; the schedule remains a ceiling rather than a central tendency.
- Permitting intermediate rises **withdraws the non-expansion invariant** the model has rested on since the earliest drafts. `I1` was restated as `S(t) ≤ S*(t) ≤ S₀`; `P3` was renamed from Non-expansion to Bounded supply, retaining its former statement as history; and the Abstract, §7.2, §7.6, §8, §9.1, and §10 were corrected accordingly. No operation in §8.1 produces an increase. The two candidate readings — a generated history that moves in ways the rules cannot produce, or an operation that restores retired and burned R, which would withdraw "permanent" from §7.5 and §7.7 — are recorded as `OPEN-39`, now the manuscript's most consequential open item.
- The simulated data comes from a generator that will be built, published on GitHub and not disclosed within the work. `OPEN-38` resolved and `OPEN-20` narrowed to that split: the whitepaper states that the series is generated and where the generator lives; the work shows the series without explaining it. The generator does not yet exist and no repository is named.
- The register holds thirty-nine items, twenty-five resolved. Updated r-whitepaper-development, r-monetary-protocol's non-expansion note, Q-001, current state, overview, and the affected catalog entries.
- The schedule values quoted for September 2025 are computed from the draft curve, not supplied by the source. No shape parameter, noise distribution, repository, mechanism for an increase, or implementation follows from these decisions. Verification passed: new-source registration, changed-page local and source links, catalog metadata, manuscript heading structure, a sweep for stale non-expansion claims, and `git diff --check`; no previously registered original was modified or rehashed.

## [2026-09-10] decision | Withdraw the 2026 depletion figure and restore non-expansion

- Registered the correction as `SRC-2026-09-10-non-expansion-restored-and-2026-anchor-withdrawn` (`f7e87d34`); a short capture, not a full export. Recorded [DEC-010](decisions/DEC-010-non-expansion-restored-and-2026-anchor-withdrawn.md).
- The 50%-remaining figure for 10 September 2026, confirmed in DEC-007, is **withdrawn**. It is no longer a fact of the world and was removed from the manuscript everywhere it appeared, including the rate-conflict analysis built on it. DEC-007 now marks it as withdrawn and retains its arithmetic as history. One dated figure remains: 30% at 17 September 2025.
- The permission for intermediate rises, confirmed in DEC-009, is **retracted** on the user's own statement that it conflicts with the non-expansion invariant. `I1` and `P3` are restored to `S_{t+1} ≤ S_t`; retirement and burn are permanent again; no operation restores destroyed R. The Abstract, §3.1, §7.2, §7.6, §8, §9.1, and §10 were reverted accordingly, and §7.4a's noise is monotone once more — variation in the speed of decline only. DEC-009 marks the retraction and keeps its analysis as history.
- `OPEN-39`, the manuscript's most consequential open item, is closed by the removal of its cause rather than by an answer. Both readings it held open are moot.
- The schedule was rebased on the single 2025 anchor. With `k = 2`, `λ = 45.57` years gives 98.8% at 1980, 95.3% at 1985, 74.0% at 2000, 46.3% at 2015, 30.0% at 2025, 17.7% at 2035, and 6.7% at 2050. The present quantity, about 28.6%, is now derived rather than stated.
- One question follows, recorded as `OPEN-40`. "30% remains" states an actual quantity, so it is a point on the realized path; the schedule sits at or above the path, and unless they meet on that date the figure pins nothing. §7.4 proceeds on the reading that they meet, which is what makes `λ` computable from `k`. If they do not, the schedule has no anchor and both parameters are free.
- The register holds forty items, twenty-seven resolved. Updated DEC-007, DEC-009, r-monetary-protocol, r-whitepaper-development, Q-001, current state, overview, and the affected catalog entries.
- The curve table is computed from the surviving anchor, not supplied by the source. No shape parameter or replacement figure is confirmed. Verification passed: new-source registration, changed-page local and source links, catalog metadata, manuscript heading structure, a sweep confirming the withdrawn figure survives nowhere as a value, a sweep for stale non-expansion wording, and `git diff --check`; no previously registered original was modified or rehashed.

## [2026-09-10] decision | Unfix the depletion figure into an acceptance criterion

- Registered the decision as `SRC-2026-09-10-depletion-as-tolerance-band` (`a3543dcb`); a single message, not a full export. Recorded [DEC-011](decisions/DEC-011-depletion-as-tolerance-band.md).
- The 30% figure is **not fixed**. The world states an acceptance criterion instead: a steady decline from 17 September 1975 to 17 September 2025, with **30 ± 5%** remaining on that date meeting it. No dated quantity is asserted anywhere, and the manuscript now publishes none.
- The curve is consequently a **family**, not a fitted line. A point fixes the scale once a shape is chosen; a band does not. For the draft shape `k = 2` the admissible scale runs from 42.47 to 48.80 years, and other shapes have their own ranges. §7.4 now shows the admissible span across the world's history — 94.6%–95.9% in 1985, 70.7%–76.9% in 2000, 41.2%–51.1% in 2015, 25%–35% in 2025, 13.6%–22.1% in 2035, 4.4%–9.4% in 2050 — in place of a single fitted table, and the previously derived present figure was removed with it.
- Because the flows are generated, a criterion rather than a value gives the generator a test to pass. Shape, scale, and noise process are jointly constrained by one condition at one date and by nothing else; any combination whose realized path lands in the band qualifies. Recorded in §7.4 as the generator's acceptance test.
- `OPEN-40` becomes moot: the tolerance accommodates a realized path running below its schedule without the two meeting, so whether they meet no longer determines whether anything is anchored. What survives is how far below, which is the noise amplitude in `OPEN-36`. `OPEN-37` is now a joint parameter choice rather than a missing anchor.
- The criterion is read as applying to the realized quantity rather than to the schedule, since that is what "remains" denotes; recorded in §7.4a.
- The register holds forty items, twenty-eight resolved. Updated DEC-009 and DEC-010 with the unfixing, r-monetary-protocol, r-whitepaper-development, Q-001, current state, overview, and the affected catalog entries.
- The admissible parameter ranges and the spread table are computed from the criterion, not supplied by the source. No shape, scale, or noise process is confirmed, and the generator does not exist. Verification passed: new-source registration, changed-page local and source links, catalog metadata, manuscript heading structure, a sweep confirming no dated quantity survives as an asserted value, and `git diff --check`; no previously registered original was modified or rehashed.

## [2026-09-10] maintenance | Consolidate the session's decisions and the remaining ones

- Rewrote `wiki/current-state.md` as a snapshot rather than an accumulation. It had grown by appended paragraphs across nine decisions and no longer read as a current model. It now carries identity, the confirmed monetary world as one consolidated account, the whitepaper's status, the open decisions in priority order, and an explicit list of what has been withdrawn or superseded so those figures and rules do not reappear as facts. The September 6 audit sections, evidence limits, and evolution were preserved.
- Consolidated `wiki/questions/Q-001-institutional-scope.md` the same way. The five appended paragraphs from successive decision turns were replaced by three sections: what the September 10 decisions settled, as a table against the original question list; the seven remaining decisions in priority order with what each blocks; and the still-unselected visitor relationship. The earlier institutional questions and the audit's review sequence were preserved as history.
- Both pages record that two of the session's decisions corrected the assistant rather than confirming it — the purpose of destruction, and the non-expansion invariant — so the distinction survives consolidation.
- Recorded `OPEN-41` in the manuscript: the System Payment definition admits only a participant account as payer, while DEC-007 confirms the initial payer is THE RESERVE. This decides whether release is the sole route by which R leaves the institution, and it gates `OPEN-13`. Also corrected a stale consequence in §4.5 which still claimed that all transfers conserve supply; a System Payment does not.
- The turn that surfaced `OPEN-41` was a question about what release is, not a decision, so no source was registered for it. The register now holds forty-one items, twenty-eight resolved.
- Updated the affected catalog entries. No claim, attribution, or status changed beyond what the nine decision pages already record, and no registered original was modified or rehashed.
